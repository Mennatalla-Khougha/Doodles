name: Create Pull Request on Main for Menna Branch

on:
 push:
    branches:
      - Menna # Replace 'your-branch' with the actual name of your branch

jobs:
 create-pull-request:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Automated PR: ${{ github.event.head_commit.message }}"
          title: "Automated PR: ${{ github.event.head_commit.message }}"
          body: "This is an automated pull request created by GitHub Actions."
          branch: "automated-pr-${{ github.run_number }}"
          base: "main" # This indicates that the pull request should merge changes into the 'main' branch
          draft: false
          labels: "automated"
          reviewers: "5XGeorgeX5" # Replace 'partner-username' with your partner's GitHub username